# URL pública del deploy. La usan metadataBase (app/layout.tsx),
# app/sitemap.ts y app/robots.ts para generar canonical y URLs absolutas.
# Copiá este archivo a .env.local y ajustá el valor.
NEXT_PUBLIC_BASE_URL=https://migration-project-from-react-to-nex.vercel.app
